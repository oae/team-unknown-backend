const WithdrawalStatus = {
  PENDING: 'pending',
  CANCELLED: 'cancelled',
  COMPLETED: 'completed',
};

const MAXIMUM_WITHDRAWAL_AMOUNT = 1000;

module.exports = {
  WithdrawalStatus,
  MAXIMUM_WITHDRAWAL_AMOUNT,
};
